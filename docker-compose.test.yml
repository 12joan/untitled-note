version: '3'

services:
  db:
    image: postgres
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust

  web:
    build: .
    environment:
      DATABASE_URL: postgres://postgres@db
      RAILS_ENV: test
    ports:
      - '3000:3000'
    depends_on:
      - db
